includes: ['layer:basic', 'layer:nginx', 'layer:apt', 'layer:php']
repo: https://github.com/battlemidget/juju-charm-dokuwiki
options:
  php-fpm:
    packages:
      - cgi
      - curl
      - gd
      - json
      - mcrypt
      - readline
